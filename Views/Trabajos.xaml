<Page x:Class="Proyecto_taller.Views.Trabajos"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Módulo de Trabajos"
      Background="Transparent">

    <Page.Resources>
        <!-- Estilos DataGrid -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E1E8ED"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F8FAFB"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="RowHeight" Value="48"/>
            <Setter Property="ColumnHeaderHeight" Value="50"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#34495E" Offset="0"/>
                        <GradientStop Color="#2C3E50" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
            <Setter Property="BorderBrush" Value="#4A5568"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                    <Setter Property="Foreground" Value="#1976D2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F5F7FA"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                </Trigger>
                <DataTrigger Binding="{Binding Estado}" Value="Pendiente">
                    <Setter Property="Background" Value="#FFF3CD"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Estado}" Value="Finalizado">
                    <Setter Property="Background" Value="#D4EDDA"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#357ABD"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DeleteButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#E74C3C"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C0392B"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#27AE60"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#229954"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="20"
                BorderBrush="#E1E8ED" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" BlurRadius="8" Opacity="0.1"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="15"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="15"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Título -->
                <StackPanel Grid.Row="0" Orientation="Horizontal">
                    <TextBlock Text="🔧 " FontSize="24" VerticalAlignment="Center"/>
                    <TextBlock Text="Gestión de Trabajos" FontSize="24" FontWeight="Bold"
                              Foreground="#2C3E50" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Botones -->
                <StackPanel Grid.Row="2" Orientation="Horizontal">
                    <Button Content="🔄 Actualizar" Command="{Binding CargarTrabajosCommand}" Style="{StaticResource ModernButton}"/>
                    <Button Content="➕ Nuevo Trabajo" Command="{Binding AgregarTrabajoCommand}" Style="{StaticResource ModernButton}"/>
                    <Button Content="🛠️ Servicios/Repuestos" Command="{Binding GestionarServiciosRepuestosCommand}" Style="{StaticResource ModernButton}"/>
                    <Button Content="✅ Finalizar" Command="{Binding FinalizarTrabajoCommand}" Style="{StaticResource SuccessButton}"/>
                    <Button Content="🗑️ Eliminar" Command="{Binding EliminarTrabajoCommand}" Style="{StaticResource DeleteButton}"/>
                </StackPanel>

                <!-- Filtros -->
                <StackPanel Grid.Row="4" Orientation="Horizontal">
                    <TextBlock Text="Filtrar:" VerticalAlignment="Center" Margin="0,0,10,0"
                              FontWeight="SemiBold" Foreground="#2C3E50"/>
                    <RadioButton Content="Todos" IsChecked="{Binding FiltroTodos}" GroupName="Filtro" Margin="5,0" VerticalAlignment="Center"/>
                    <RadioButton Content="Pendientes" IsChecked="{Binding FiltroPendientes}" GroupName="Filtro" Margin="5,0" VerticalAlignment="Center"/>
                    <RadioButton Content="En Progreso" IsChecked="{Binding FiltroEnProgreso}" GroupName="Filtro" Margin="5,0" VerticalAlignment="Center"/>
                    <RadioButton Content="Finalizados" IsChecked="{Binding FiltroFinalizados}" GroupName="Filtro" Margin="5,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- DataGrid -->
        <Border Grid.Row="2" Background="White" CornerRadius="8"
                BorderBrush="#E1E8ED" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="Black" Direction="270" ShadowDepth="3" BlurRadius="15" Opacity="0.15"/>
            </Border.Effect>

            <DataGrid ItemsSource="{Binding Trabajos}" 
                      SelectedItem="{Binding TrabajoSeleccionado}" Margin="1">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding TrabajoID}" Width="70" IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="#1976D2"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Vehículo" Binding="{Binding Vehiculo.Marca}" Width="120"/>
                    <DataGridTextColumn Header="Modelo" Binding="{Binding Vehiculo.Modelo}" Width="120"/>
                    <DataGridTextColumn Header="Placa" Binding="{Binding Vehiculo.Placa}" Width="100"/>
                    <DataGridTextColumn Header="Cliente" Binding="{Binding Vehiculo.Cliente.Nombre}" Width="*"/>
                    <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" Width="*"/>
                    <DataGridTextColumn Header="Tipo" Binding="{Binding TipoTrabajo}" Width="120"/>
                    <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Fecha Ingreso" Binding="{Binding FechaIngreso, StringFormat=dd/MM/yyyy}" Width="130"/>
                    <DataGridTextColumn Header="Precio Est." Binding="{Binding PrecioEstimado, StringFormat=Bs. {0:N2}}" Width="110"/>
                    <DataGridTextColumn Header="Precio Final" Binding="{Binding PrecioFinal, StringFormat=Bs. {0:N2}}" Width="110"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>