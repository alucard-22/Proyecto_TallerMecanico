<Page x:Class="Proyecto_taller.Views.Trabajos"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Módulo de Trabajos"
      Background="{StaticResource BgPrimary}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ── Header / Toolbar ── -->
        <Border Grid.Row="0" Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="14"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="14"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Título -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="38" Height="38" CornerRadius="8" Margin="0,0,12,0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#6366F1" Offset="0"/>
                                <GradientStop Color="#8B5CF6" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <TextBlock Text="🛠️" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Gestión de Trabajos" Style="{StaticResource PageTitle}"/>
                        <TextBlock Text="Administra órdenes de servicio y reparaciones"
                                   Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,2,0,0"/>
                    </StackPanel>
                </StackPanel>

                <!-- Botones -->
                <StackPanel Grid.Row="2" Orientation="Horizontal">
                    <Button Content="🔄 Actualizar"           Command="{Binding CargarTrabajosCommand}"              Style="{StaticResource BtnSecondary}"/>
                    <Button Content="➕ Nuevo Trabajo"          Command="{Binding AgregarTrabajoCommand}"              Style="{StaticResource BtnPrimary}"/>
                    <Button Content="🛠️ Servicios y Repuestos" Command="{Binding GestionarServiciosRepuestosCommand}" Style="{StaticResource BtnWarning}"/>
                    <Button Content="✅ Finalizar"              Command="{Binding FinalizarTrabajoCommand}"            Style="{StaticResource BtnSuccess}"/>
                    <Button Content="🗑️ Eliminar"               Command="{Binding EliminarTrabajoCommand}"             Style="{StaticResource BtnDanger}"/>
                </StackPanel>

                <!-- Filtros -->
                <Border Grid.Row="4"
                        Background="{StaticResource BgCard}"
                        CornerRadius="6" Padding="12,8">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Estado:"
                                   Foreground="{StaticResource TextSecondary}"
                                   FontWeight="SemiBold" FontSize="12"
                                   VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <RadioButton Content="📋 Todos"       IsChecked="{Binding FiltroTodos}"       GroupName="FiltroTrab" VerticalAlignment="Center"/>
                        <RadioButton Content="⏳ Pendientes"  IsChecked="{Binding FiltroPendientes}"  GroupName="FiltroTrab" VerticalAlignment="Center"/>
                        <RadioButton Content="🔄 En Progreso" IsChecked="{Binding FiltroEnProgreso}"  GroupName="FiltroTrab" VerticalAlignment="Center"/>
                        <RadioButton Content="✅ Finalizados"  IsChecked="{Binding FiltroFinalizados}" GroupName="FiltroTrab" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- ── DataGrid ── -->
        <Border Grid.Row="2"
                Background="{StaticResource BgSurface}"
                CornerRadius="10"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="1">
            <DataGrid x:Name="dgTrabajos"
                      ItemsSource="{Binding Trabajos}"
                      SelectedItem="{Binding TrabajoSeleccionado}"
                      Margin="0">
                <!-- ComboBox de Estado dentro del DataGrid -->
                <DataGrid.Resources>
                    <Style x:Key="EstadoComboBox" TargetType="ComboBox">
                        <Setter Property="Background"      Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding"         Value="6,3"/>
                        <Setter Property="FontWeight"      Value="SemiBold"/>
                        <Setter Property="FontSize"        Value="11"/>
                        <Setter Property="Foreground"      Value="{StaticResource TextPrimary}"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Estado}" Value="Pendiente">
                                <Setter Property="Background" Value="{StaticResource RowPending}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Estado}" Value="En Progreso">
                                <Setter Property="Background" Value="{StaticResource RowInProgress}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Estado}" Value="Finalizado">
                                <Setter Property="Background" Value="{StaticResource RowDone}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding TrabajoID}" Width="60" IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentLight}"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Vehículo" Binding="{Binding Vehiculo.Marca}"  Width="110">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Modelo"  Binding="{Binding Vehiculo.Modelo}" Width="110"/>
                    <DataGridTextColumn Header="Placa"   Binding="{Binding Vehiculo.Placa}"  Width="90">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontFamily" Value="Consolas"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Cliente"     Binding="{Binding Vehiculo.Cliente.Nombre}" Width="*"/>
                    <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}"             Width="*"/>
                    <DataGridTextColumn Header="Tipo"        Binding="{Binding TipoTrabajo}"              Width="110"/>

                    <!-- Estado editable -->
                    <DataGridTemplateColumn Header="Estado" Width="130">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ComboBox SelectedValue="{Binding Estado, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          Style="{StaticResource EstadoComboBox}"
                                          SelectionChanged="EstadoComboBox_SelectionChanged">
                                    <ComboBoxItem Content="⏳ Pendiente"  Tag="Pendiente"/>
                                    <ComboBoxItem Content="🔄 En Progreso" Tag="En Progreso"/>
                                    <ComboBoxItem Content="✅ Finalizado"  Tag="Finalizado"/>
                                </ComboBox>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Fecha Ingreso"
                                        Binding="{Binding FechaIngreso, StringFormat=dd/MM/yyyy}"
                                        Width="110">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                <Setter Property="FontSize"   Value="11"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="P. Estimado"
                                        Binding="{Binding PrecioEstimado, StringFormat=Bs. {0:N2}}"
                                        Width="110">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="P. Final"
                                        Binding="{Binding PrecioFinal, StringFormat=Bs. {0:N2}}"
                                        Width="110">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Foreground" Value="#6EE7B7"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
