<!-- NUEVA_RESERVA_WINDOW.XAML -->
<Window x:Class="Proyecto_taller.Views.NuevaReservaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nueva Reserva"
        Height="680" Width="580"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BgPrimary}">

    <Border Background="{StaticResource BgSurface}"
            CornerRadius="12" Margin="16" Padding="28">
        <Border.Effect>
            <DropShadowEffect Color="Black" Direction="270" ShadowDepth="4" BlurRadius="20" Opacity="0.4"/>
        </Border.Effect>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="📅 Nueva Reserva"
                           Style="{StaticResource PageTitle}" Margin="0,0,0,6"/>
                <TextBlock Text="Complete la información para crear una nueva reserva"
                           Foreground="{StaticResource TextMuted}" FontSize="12" Margin="0,0,0,24"/>

                <TextBlock Text="Vehículo (Cliente — Vehículo) *" Style="{StaticResource FormLabel}"/>
                <ComboBox x:Name="cmbVehiculo" Margin="0,0,0,16"/>

                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="14"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Fecha de la Cita *" Style="{StaticResource FormLabel}"/>
                        <DatePicker x:Name="dpFecha"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Hora *" Style="{StaticResource FormLabel}"/>
                        <ComboBox x:Name="cmbHora">
                            <ComboBoxItem Content="08:00"/>
                            <ComboBoxItem Content="09:00"/>
                            <ComboBoxItem Content="10:00"/>
                            <ComboBoxItem Content="11:00"/>
                            <ComboBoxItem Content="12:00"/>
                            <ComboBoxItem Content="14:00"/>
                            <ComboBoxItem Content="15:00"/>
                            <ComboBoxItem Content="16:00"/>
                            <ComboBoxItem Content="17:00"/>
                            <ComboBoxItem Content="18:00"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Tipo de Servicio *" Style="{StaticResource FormLabel}"/>
                <ComboBox x:Name="cmbTipoServicio" Margin="0,0,0,14">
                    <ComboBoxItem Content="Mecánica" IsSelected="True"/>
                    <ComboBoxItem Content="Electricidad"/>
                    <ComboBoxItem Content="Pintura"/>
                    <ComboBoxItem Content="Laminado"/>
                    <ComboBoxItem Content="Cambio de Aceite"/>
                    <ComboBoxItem Content="Alineación y Balanceo"/>
                    <ComboBoxItem Content="Mantenimiento Preventivo"/>
                    <ComboBoxItem Content="Diagnóstico"/>
                    <ComboBoxItem Content="Otro"/>
                </ComboBox>

                <TextBlock Text="Prioridad" Style="{StaticResource FormLabel}"/>
                <ComboBox x:Name="cmbPrioridad" Margin="0,0,0,14">
                    <ComboBoxItem Content="⚪ Normal"  Tag="Normal"  IsSelected="True"/>
                    <ComboBoxItem Content="🔴 Urgente" Tag="Urgente"/>
                    <ComboBoxItem Content="⭐ VIP"     Tag="VIP"/>
                </ComboBox>

                <TextBlock Text="Precio Estimado (Opcional)" Style="{StaticResource FormLabel}"/>
                <TextBox x:Name="txtPrecioEstimado" Margin="0,0,0,14"/>

                <TextBlock Text="Observaciones" Style="{StaticResource FormLabel}"/>
                <TextBox x:Name="txtObservaciones"
                         Height="72" TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         Margin="0,0,0,28"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="✓  Guardar"  Click="Guardar_Click"   Style="{StaticResource BtnSuccess}" Padding="0,13" FontSize="14" Margin="0"/>
                    <Button Grid.Column="2" Content="✕  Cancelar" Click="Cancelar_Click"  Style="{StaticResource BtnDanger}"  Padding="0,13" FontSize="14" Margin="0"/>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Border>
</Window>
